dots - Dotfile management made less toilesome.

USAGE:
    dots [OPTIONS] <SUBCOMMAND>

FLAGS:
    -h, --help    Prints help information

OPTIONS:
        --store <PATH>    Path of dotfile store [default: ~/.dotfiles]

SUBCOMMANDS:
    help         Prints this message or the help of the given subcommand(s)
    install      Link dotfile to target in home directory
    list         List all installed dotfiles in the given store
    manage       Move dotfile to the store and link it back to its target
    uninstall    Remove dotfile target in home directory
    unmanage     Move dotfile out of the store and back to its target
